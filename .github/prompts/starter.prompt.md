---
description: Start or continue the active ORTEC abapGit topic from concise persistent state
agent: ortec-abapgit-orchestrator
argument-hint: Optional topic or slice override
---

Read `.memory/state.md` first.

If the user supplies a topic or slice, use it. Otherwise continue the topic under
`## Active topic`.

For `variant-b-partial-clone`, use `.github/prompts/variant-b.prompt.md` as the
owner-approved specification. Read only the active links and exact source files
required for the current slice. Do not resume H4 independently, do not repeat
broad archaeology, and do not restore progressive-deepen recovery.

Before productive code changes, enforce the correctness review and performance
`DESIGN_GATE` recorded in the active state.
